# OMO – Frontend Only (React + Vite + TypeScript)

Frontend-only prototype of the OMO mobility experience built with React, TypeScript, Vite, Context API, Framer Motion, React Router, and CSS Modules.

## Getting started

```bash
npm install
npm run dev
```

Then open the printed local URL in your browser.

